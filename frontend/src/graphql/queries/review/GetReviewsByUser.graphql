query GetReviewsByUser($userId: String!) {
  getReviewsByUser(userId: $userId) {
    id
    rating
    content
    announcement {
      name
      price
    }
    reviewer {
      displayName
      avatar
    }
    createdAt,
    updatedAt
  }
}

query GetAnnouncementCharacteristics($id: String!) {
  getAnnouncementCharacteristics(id: $id) {
    characteristics {
      group
      data {
        value
        characteristic
        unitSuffix
        type
      }
    }
  }
}